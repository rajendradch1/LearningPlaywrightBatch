let age = 20;

let is_pramod_will_go_to_goa = age > 18 ? "Yes, let go goa!" : "no you are minor, not allowed";
console.log(is_pramod_will_go_to_goa);
// Ternary = condition ? "true result" : "false result" ;
