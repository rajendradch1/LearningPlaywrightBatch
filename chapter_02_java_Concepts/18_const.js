console.log(max_retry) ; // tdz
const max_retry = 4;
//max_retry = 5 ;  const not allow to reassign value. it will throw error.
const score = 100;
//score = score + 10; // const value cannot be changed. it will throw error.

//where const use
const pi = 3.14; 
const prod_api_url = "https://app.vow.com/#login";

//means we can not reassign the value of constant
