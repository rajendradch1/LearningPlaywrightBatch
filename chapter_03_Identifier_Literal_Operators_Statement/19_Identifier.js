/**
let class = "Hello World" 
//class is a reserve reserverd keyword in javaScript, so it can not be used as identifier. This will result in syntax error.

let return = 5;
//class is a reserve reserverd keyword in javaScript, so it can not be used as identifier. This will result in syntax error.

let myVariable = 10 ;
// This is valid identifier and can be used without error

// They look like keywords but are not reserved
let undefined = 5; // Work but Never do this!
let Infinity = 10; // Work but Never do this!
*/