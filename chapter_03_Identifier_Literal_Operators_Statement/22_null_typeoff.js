// Null - "intentionally nothing"

let selectedItem = null;
let searchResult = null;

let declaredOnly; // undefined not yet assigned. 

console.log(searchResult); // created container and adding null as value
console.log(declaredOnly); // created container but not defined any value.


// Equality comparision
console.log(null == undefined);
