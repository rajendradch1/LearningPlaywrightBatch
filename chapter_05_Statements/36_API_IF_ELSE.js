// when ever we work with api we get status code like 200, 404

let statusCode = 200; // APIs working fine

if (statusCode == 200){
    console.log("Working fine!");
} else if (statusCode == 404){
    console.log("not found");
} else {
    console.log("not matching status code");
}

